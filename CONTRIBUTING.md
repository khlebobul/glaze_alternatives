# Contributing

Add standalone products to `data/apps.json`. Add Glaze Store apps and product mappings to `data/glaze-apps.json`.

Use stable official URLs, working square icons, and short factual descriptions. Only map apps with substantially overlapping core functionality. Categories: `productivity`, `developer`, `media`, `utilities`, `design`.

Run `npm test` and `npm run build`, then open a pull request.
